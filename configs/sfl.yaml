defaults:
  - env: entity
  - learning: 
    - ppo-base
    - ppo-rnn
  - misc: misc
  - ued: sfl
  - env_size: m
  - eval: m
  - train_levels: random
  - model: 
    - model-base
    - model-transformer
    - model-perm-invariant-mlp
  - _self_

eval:
  eval_freq: 64
learning:
  num_steps: 256
seed: 0